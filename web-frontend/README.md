# 4D Strategy Frontend

React SPA with TanStack Router and TanStack Query. Uses **bun** (not npm).

## Setup

```bash
bun install
```

## Dev

```bash
bun run dev
```

Runs on http://localhost:5173. API requests are proxied to the FastAPI backend (port 8000).

## Build

```bash
bun run build
```
